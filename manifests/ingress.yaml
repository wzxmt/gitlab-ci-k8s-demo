---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: presentation-gitlab-k8s-__CI_ENVIRONMENT_SLUG__
  namespace: gitlab
  labels:
    app: presentation-gitlab-k8s
    ref: __CI_ENVIRONMENT_SLUG__
  annotations:
    kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "traefik"
spec:
  tls:
  - hosts:
    - __CI_ENVIRONMENT_SLUG__-gitdemo-k8s.qikqiak.com
    # the secret used here is an unsigned wildcard cert for demo purposes
    # use your own or comment this out
    secretName: tls-wildcard-demo
  rules:
  - host: __CI_ENVIRONMENT_SLUG__-gitdemo-k8s.qikqiak.com
    http:
      paths:
      - path: /
        backend:
          serviceName: gitdemo-k8s-__CI_ENVIRONMENT_SLUG__
          servicePort: 8000
